function scrollLeft() {
    const imagePanel = document.getElementById('image-panel');
    imagePanel.scrollLeft -= 200; /* 200 piksel sola kaydır */
  }
  
  function scrollRight() {
    const imagePanel = document.getElementById('image-panel');
    imagePanel.scrollLeft += 200; /* 200 piksel sağa kaydır */
  }
   